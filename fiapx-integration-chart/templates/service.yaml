apiVersion: v1
kind: Service
metadata:
  name: {{ include "fiapx-integration-chart.fullname" . }}
  labels:
    {{- include "fiapx-integration-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fiapx-integration-chart.selectorLabels" . | nindent 4 }}
